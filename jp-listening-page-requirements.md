# jp-listening-page 改动需求文档（待执行，持续补充中）

> 状态：需求1~6已实现并应用到 `tools/listening/build_page.py`，`docs/private/n2-listening/2021-07/index.html`
> 已用新模板重新生成，等用户打开检查效果、确认没问题或提出改动。2021-12 那条流水线仍暂停。

## 背景

正在跑 `2021年12月N2.mp3` 听力页（沿用 `docs/private/n2-listening/2021-07/` 先例：
`docs/private/n2-listening/2021-12/`，标题「N2听力精听：2021年12月真题」，密码复用
2021-07 的哈希 `bb6db7538afe36c647632bf9a561cf15101756f132337f1d650ac8cd95cc3c81`，不进导航，
全量处理，不再逐项跟用户确认第0步）。转写已在后台跑完（Task #1），后续步骤（标边界、
两轮 Agent、合并、精修、生成页面）暂停，等下面的交互改动定下来再继续。

## 需求 1：去掉所有播放按钮，改成点标题/内容即可播放

现状：每句有独立的「▶ 再生」「↺ もう一度」按钮；每小题 h3 标题旁有「▶ この問題を再生」；
每大题 h2 标题旁有「▶ 大問を再生」。

改动目标：

- 去掉这些按钮本身。
- 点击句子卡片（假名+翻译+笔记那一整块内容）→ 播放这一句，每次点击都从头播（不再有
  "点第二下暂停"的切换逻辑）。
- 点击 h3 小题标题 → 播放整个小题（该小题下所有句子按顺序连播）。
- 点击 h2 大题标题 → 播放整个大题（该大题下所有句子按顺序连播）。
- 暂停/继续统一交给右下角悬浮迷你播放器的 ▶/⏸ 按钮，卡片/标题本身只负责"（重新）开始播放"。
- 视觉提示：卡片和标题加 `cursor:pointer` + hover 反馈（边框/背景变化），再加一个 hover
  时才淡入的小"▶"提示，平时不占视觉空间、不是常驻按钮，但保留"这里能点"的可发现性。
- `.seg-card.playing` 高亮效果保留（序列播放时跟着高亮当前句、自动滚动）。
- 侧边小题目录（`.toc`/`.toc-float` 里的小问列表）**保持现状**，只负责滚动定位，不触发
  播放——除非后续需求里用户改主意要求联动。

## 需求 2：迷你播放器的循环开关改成全局持续开关

现状：`playFrom()` 每次开始新播放都会把 `player.loop` 强制重置为 `false`，所以循环状态
不会跨越"点了新的一句/新的一题"存活。

改动目标：去掉这个强制重置。用户在迷你播放器点亮 ⟲ 循环后，不管接下来点哪句/哪题/哪个
大题，都保持循环播放当前内容，直到用户自己再点一次 ⟲ 关掉。

## 需求 3：把新交互套回已发布的 2021-07 页面

不止改模板给以后生成新页面用，`docs/private/n2-listening/2021-07/` 这个已经生成过的页面
也要重新生成一遍应用新交互。

- 不需要重新转写/重新切音频——`audio/` 目录下 308 段音频已经存在且正确，原始素材
  `enriched_final5.json`（当时最终跑出来的合并结果）和源音频 `2021年7月N2.mp3` 都还在
  旧 session 的 scratchpad/Downloads 里，可以直接拿来重新生成 `index.html`。
- 计划给 `build_page.py` 加一个"跳过切片"的判断：`audio/` 目录下已有全部预期文件时
  就不再调用 `cut_segments`，只重新渲染 HTML/CSS/JS。
- 密码复用旧哈希（`--password-hash`），标题/副标题不变。

## 需求 4：页面整体排版收紧，去掉序号徽章

现状：每句卡片左上角有「001」「002」这样的三位数序号徽章（`.seg-num`）；`.seg-card`/
`.post-body h2`/`.post-body h3` 的内边距和行距偏松散。

改动目标：
- 去掉句卡片上的 001/002/003 序号徽章。
- 收紧 `.seg-card` 的内边距/行距、`.post-body h2`/`.post-body h3` 的上下 margin，
  让一屏能看到更多句子内容，不需要频繁滚动。

## 需求 5：底部迷你播放器条加上一个/下一个/最前/最后导航

不是另开弹层，就是需求1里已有的那条右下角常驻迷你播放器（点标题/内容时它出现/更新）。
在现有的暂停/播放/循环/关闭基础上，加"上一个/下一个/最前/最后"导航按钮：按当前播放的
是哪个级别（大题/小题/句子），能在同级之间切换播放目标。导航范围严格限制在同级内、
到头（第一个/最后一个）就禁用/不生效，不跨级；切换目标后立刻自动播放新目标，不用
用户再点一次播放。

## 需求 6：迷你播放器导航切换目标时，页面要跟着联动

用迷你播放器的"上一个/下一个/最前/最后"切到新目标（不管是切句子、切小题、还是切大题）
时，页面不能只是后台换音频，要跟着做相应联动：
- 切到别的大题（跨 Tab）：页面的 Tab 要自动切换过去，显示新大题的内容（跟现在点顶部
  Tab 按钮的效果一样）。
- 不管切到哪一级，当前正在播放的那句话都要在页面上高亮（复用 `.seg-card.playing` 效果），
  并自动滚动到屏幕可见区域内（`scrollIntoView`），不用用户自己找。

---

## 需求 7：播放自然结束后迷你播放器不消失，点别处/点✕才关掉

现状：不管是主动点✕停止，还是一段内容自然播完（没开循环），`stopPlayer()` 都会把迷你
播放器整个隐藏掉。

改动目标：播完（没循环）只进入"播完"状态，播放条本身不消失——图标变回▶（可以重新点
播放，从头开始）、上一个/下一个导航仍然可用；只有点✕，或者点了播放条以外的任何地方，
才真正隐藏播放条。已实现：`finishPlayer()` 区分"播完待命"和`stopPlayer()`"彻底关闭"，
外加一个 document 级别的点击监听，`player.finished` 为真且点击目标不在 `#miniPlayer`
内时才调用 `stopPlayer()`。

## 已确认的细节（原"待确认"里的疑点，逐条拍板）

- 侧边小题目录点击：不触发播放，只负责滚动定位。
- 句卡片点击 vs 划词复制文本冲突：接受这个取舍，不做特殊处理。
- 迷你播放器"上一个/下一个"导航范围：严格同级，到头（第一个/最后一个）就禁用，不跨级。
- 切换导航目标后：立刻自动播放新目标，不用户再点一次播放。

## 待确认的悬而未决问题

（暂无，等继续补充需求）
