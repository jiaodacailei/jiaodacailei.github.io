# textbook-sjp-zg-l13 操作日志

补记（本文件是后来才建立的，以下是根据当天会话回溯整理的记录，格式不如
后续条目详细）。

## 本次会话涉及的修复（在原始生成之后，均为后续反馈修复）

- 用 `audit_furigana.py --all` 重新全量复核（工具修复零覆盖bug之后，见
  l14 日志），89条生词逐条人工核对，发现"後方づけ"疑似typo（后确认原文
  正确，用户指出教材截图本来就是"後方づけ"，之前误改成"後片付け"已
  `git revert` 撤销）。
- 用户反馈"出生率"注音错误（原读しゅっせいりつ，应为しゅっしょうりつ，
  跟生词条目"出生"读音一致），加进 `_TOKEN_READING_OVERRIDES_UNCONDITIONAL`
  无条件覆盖表，patch 了句子 id25/30。
- 用户反馈"出産数"注音错误（原读かず，应为すう），加了一条通用规则：
  "数"直接接在另一个汉字词后面（没有"の"分隔）构成统计学复合词时读すう，
  跟"子供の数"这种"の"+数的独立名词用法（读かず）区分开，不是只改这一个
  词。patch 了句子 id29。
- `check_clip_drift.py` 核实过 l13 生词表没有 l14 那种系统性裁剪过猛问题
  （0条drift）。
- 用户反馈"取り組みを行っている"注音错误（"行"原读い，应为おこな），
  加了一条规则：紧跟在助词"を"后面、且 pykakasi 默认读音确实猜成"行く"
  分支（hira 以い开头）时纠正成おこな，不影响"行い"（ます形词干）这类
  pykakasi 本来就猜对的活用形。用 `tokenize_ja()` 测试过"会社に行った"/
  "銀行に行く"这类真正表示"去"的用法不受影响，重跑全量复核确认无回归。
  patch 了句子 id30。
- 用户要求新增规则："生词表里有的词，会话/课文里出现时注音直接用生词表的
  读音"（触发案例：生词"年月"读ねんげつ，课文句子"長い年月が必要だろう"
  里 pykakasi 默认猜成としつき，不一致）。改成了 `build_page.py` 里的通用
  机制（`tokenize_ja()` 新增 `vocab_readings` 参数，`build_lesson_data()`/
  `patch_sentence_tokens.py` 自动构造并传入），不是一次性改这一个词。用
  脚本扫过 l13/l14 全部句子，确认只有这一处受影响，已 patch 句子 id31。
  同步更新了 `audit_furigana.py` 让审核逻辑也用同一套映射，避免审核脚本
  和真实渲染逻辑再次出现分歧。
- 用户指出会话/课文的填空题覆盖率太低（当时15句会话0句有填空，16句课文
  只有1句），追问"你有没有好好做过？"——确认没有真的逐句判断。用户明确
  规则：有语法点的句子挖语法点，没有语法点的句子挖主要动词/名词，每句
  都必须有填空。逐句重新过了一遍（含原来已有的id4/31），新增/补全了29句
  会话+16句课文的 `blanks`（部分同步补了 `notes`），会话/课文合计
  15/15+16/16 全覆盖。挖空文字全部用代码验证过是原文精确子串。
