# 功能设计：选中句子中的一段文字，循环复读

## 1. 需求

跟读模式下，用户可以在一句话（会话/课文，带 `char_times` 逐词时间戳的句子）
里选中一段连续的文字（不是整句），单独循环播放这一段，用来反复磨一句里
听不清/说不利索的某个短语，不用每次都从整句开头听起。

**适用范围**：只有会话/课文的句子支持（`data.js` 里 `tokens` 数组带 `t`
字段的那些）。生词条目没有逐词时间戳（一个词条通常本身就是最小单位），
不提供这个功能。

## 2. 交互流程

### 2.1 进入"选段"模式

每张带 `char_times` 的会话/课文卡片右下角新增一个常驻但默认隐藏的小图标
（跟 `edit-mode.js` 的 ✎ 图标同一个模式：`display:none`，靠设置面板里的
开关整体控制显示/隐藏，图标本身 `stopPropagation`，不触发卡片默认的整句
播放）。暂定图标 🔁，class 定为 `.seg-repeat-icon`，具体符号/位置可以再看
实际效果调整。

设置面板新增一个开关（复用 `edit-mode.js` 里"内容编辑"那组的样式），
比如"🔁 选段复读"，打开后所有符合条件的卡片才会显示这个图标。**默认关闭**
——平时跟读模式下卡片右下角不多一个图标，避免误触。

### 2.2 选起点/终点（点词-点词，不是拖拽选择）

1. 点了卡片上的 🔁 图标 → 这张卡片进入"选段中"状态（比如卡片加一个
   `.seg-selecting` class，视觉上提示"请点选起始词"）。
2. 用户点句子文字里的第一个词（`.tw` 元素，也就是已经有 `data-t` 属性、
   参与跟读高亮的那些 token）→ 记为起点，这个词加一个"已选中起点"的
   样式（比如虚线下划线），继续等待点第二个词。
3. 用户点第二个词（可以在起点前面也可以在后面，不要求点击顺序等于
   文字顺序，按 token 在句子里的实际先后位置自动排序）→ 确定终点，
   立刻开始播放选中范围，并且**自动循环**（复用悬浮播放条已有的
   循环开关状态，不额外弹一次询问）。
4. 选段完成后卡片退出"选段中"状态（`.seg-selecting` 去掉），改成显示
   "当前选中范围"的样式（比如选中的这几个词加一层浅色背景，直到悬浮
   播放条关闭才消失）。

**取消/重新选**：
- 点了起点、还没点终点时，再点一下 🔁 图标（或者点了别的卡片的 🔁）→
  取消当前待定的起点，可以重新开始。
- 只点了一个词就直接点起点=终点相同的词 → 允许，就是单独复读这一个词。

### 2.3 播放范围怎么算

- **起点时间**：选中范围里位置最靠前那个 token 的 `t`。
- **终点时间**：选中范围里位置最靠后那个 token 的**下一个** token 的
  `t`（也就是"这个词读完、下一个词开始读之前"那个时间点）；如果选中
  的终点已经是这句最后一个 token，终点时间就用这句音频本身的自然结尾
  （`<audio>` 的 `duration`，或者直接播放到 `ended` 不用手动截断）。
- 这跟现有跟读高亮"当前时间 ≥ 某词的 t 就高亮这个词"是同一套逻辑，
  不需要额外算法，只是换算方向反过来（从"选中了哪几个词"反推"这段
  时间范围"）。

### 2.4 播放控制（完全复用悬浮迷你播放条，不新增播放UI）

选段确定的瞬间，就是调用现有播放器入口，只是多传一个"播放范围"：

- **播放/暂停**：复用 `mpPlayPause`，行为不变。
- **循环**：复用 `mpLoop`。开着循环 = 放到范围终点自动跳回起点继续放
  （无限循环，直到用户自己关掉循环或者点停止）；不开循环 = 放到范围
  终点就停在那，不会往下播这句剩下的内容（这是跟"整句播放不循环时
  自然播完"最大的行为差异，需要特别处理，见下面第4节）。
- **停止（✕）**：点了就整个退出选段播放——迷你播放条消失、卡片上
  "选中范围"的高亮样式也一起清掉，卡片恢复成"点一下播整句"的默认
  行为。**这是退出选段模式的唯一方式**（对应最早讨论时说的"播放条
  退出，本次选择也退出"）。
- **上一个/下一个/最前/最后**（`mpFirst`/`mpPrev`/`mpNext`/`mpLast`）：
  选段场景下没有"上一个片段"的概念，这四个按钮直接禁用（跟现在"只有
  一句可播、没有同级兄弟节点"时的禁用逻辑一样，不用新写判断）。
- **播放条上的文字**（`mpScope`/`mpPos`）：`mpScope` 显示选中这段文字
  本身（比如直接截取 token 的 text 拼起来，太长可以截断加省略号）；
  `mpPos` 不显示内容（没有"第几个/共几个"的意义）。

### 2.5 跟读高亮

播放范围内，`.tw-active` 高亮逻辑完全不用改——反正就是拿 `audio.
currentTime` 跟每个 `.tw[data-t]` 比，选段播放时 `currentTime` 就只会在
`[起点,终点)` 这个小范围里打转，高亮自然只会落在选中的这几个词上，
不需要额外代码。

## 3. 数据层面

不需要改 `data.js`/`build_page.py` 生成逻辑——`t` 字段已经是逐词时间戳，
选段功能纯粹是前端交互 + 播放控制层面的事，不涉及内容生成流程。

**唯一要注意**：这个功能只能在带 `t` 字段的卡片上出现——`page-renderer.js`
渲染时本来就知道哪些 token 有 `t`（有 `char_times` 才会给 token 加 `t`），
生成 🔁 图标时按同样的条件判断（这张卡片是否存在至少一个带 `data-t` 的
`.tw`），生词卡片天然不会有任何 `.tw[data-t]`，图标自动不出现，不用
额外传参区分。

## 4. `listening-page.js` 改造点（复用 `player` 全局状态，不新建一套）

现有 `player` 对象：
```js
var player = {
  audios: [], idx: 0, loop: false, active: false, finished: false, scopeLabel: "",
  navType: null, navSiblings: [], navIndex: -1
};
```

新增两个字段：
```js
rangeStart: null,   // 选段模式：范围起点时间（秒），非选段模式为 null
rangeEnd: null,      // 选段模式：范围终点时间（秒）
```

`playScope()` 现在的调用方式是"传一堆 `<audio>` 元素，从头到尾依次播完"，
选段场景只有一个 `<audio>`、只播其中一段，不适合硬塞进现有 `playScope()`
的参数形状，计划新增一个专门的入口，比如 `playRange(audio, startT, endT,
label)`：

```js
function playRange(audio, startT, endT, label) {
  player.audios.forEach(function(a) { a.onended = null; a.pause(); });
  player.audios = [audio];
  player.idx = 0;
  player.finished = false;
  player.scopeLabel = label;
  player.navType = null;        // 没有同级兄弟节点，导航按钮自动禁用
  player.navSiblings = [];
  player.navIndex = -1;
  player.rangeStart = startT;
  player.rangeEnd = endT;
  audio.currentTime = startT;
  audio.onended = null;         // 选段模式不依赖 onended 判断"播完"
  audio.play();
  setPlayingCard(audio);
  updateMiniPlayer();
  startWordHighlight();
}
```

`tickWordHighlight()`（本来就是每帧跑一次的 RAF 循环，用来算高亮词）
顺手加一段范围边界检查：

```js
if (player.rangeEnd !== null && a && a.currentTime >= player.rangeEnd) {
  if (player.loop) {
    a.currentTime = player.rangeStart;
  } else {
    a.pause();
    player.finished = true;     // 复用现有"播完"状态，播放条按钮表现一致
    updateMiniPlayer();
  }
}
```

`stopPlayer()` 里补上清空 `rangeStart`/`rangeEnd`（跟清空 `audios` 一起），
同时触发清除卡片上"选中范围"高亮样式的逻辑（比如去掉所有
`.tw.seg-range-selected` 的 class）。

## 5. 需要新增/确认的 CSS

- `.seg-repeat-icon`：常驻但默认 `display:none`，跟 `.seg-edit-icon` 一样
  用 `body.repeat-mode-on .seg-repeat-icon { display:flex }` 之类的开关
  控制显示。
- `.seg-card.seg-selecting`：选段中（已点起点，等待点终点）的卡片提示态。
- `.tw.seg-range-pending`：已点选的起点词，等待选终点时的样式（比如
  虚线下划线）。
- `.tw.seg-range-selected`：选段确定后，范围内所有词的持续高亮样式（跟
  `.tw-active`"当前正在读的这一个词"的高亮要能同时存在、视觉上能区分——
  比如 `seg-range-selected` 是浅色背景，`tw-active` 另外叠加一层更显眼的
  颜色/加粗）。

## 6. 待确认/待定项（实现前需要拍板）

1. 🔁 图标具体用什么符号、放卡片右下角的什么位置（跟 ✎ 编辑图标会不会
   挤在一起——如果编辑模式和选段模式同时打开，两个图标要能共存不重叠）。
2. 设置面板里这个开关的具体文案。
3. "选中范围"高亮样式的具体配色，要跟现有 `.tw-active` 高亮、`.seg-notes`
   填空模式的蓝色系配色协调，不要混乱。
4. 移动端（窄屏）触屏点击 `.tw` 这种行内小元素，点击热区够不够大——
   现有跟读高亮的 `.tw` 本来就没有专门为点击优化过（原来只是纯展示用的
   span），可能需要给 `.tw` 补一点 `padding`/最小点击区域，尤其手机上。
